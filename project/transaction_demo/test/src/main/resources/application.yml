spring:
  application:
    name: tranctional-server
  datasource:
    username: root
    password: 1234
    url: jdbc:mysql://localhost:3306/db_tranctional_test

server:
  port: 8081
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml